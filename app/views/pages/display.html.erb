<script>
  var mapAccessKey = "<%= ENV['MAP_ACCESS_KEY'] %>";
  // export MAP_ACCESS_KEY= in bash session & will need to enter into Heroku when deployed
</script>

<h1>Display page</h1>
<ul>
<% @stops.each_index do |index| %>

<p><%= " #{index+1} #{@stops[index].name} , arrived on #{@stops[index].arrived_at}" %></p>

<% end %>
</ul>

<div id="map">

</div>

<script>

var tripID = <%= @trip_id%> ;
</script>